/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type ClaimRewardInstructionAccounts = {
  user: Signer;
  vaultAuthority: Signer;
  nftStake: PublicKey | Pda;
  mint: PublicKey | Pda;
  userToken: PublicKey | Pda;
  vaultToken: PublicKey | Pda;
  tokenProgram?: PublicKey | Pda;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type ClaimRewardInstructionData = { discriminator: Array<number> };

export type ClaimRewardInstructionDataArgs = {};

export function getClaimRewardInstructionDataSerializer(): Serializer<
  ClaimRewardInstructionDataArgs,
  ClaimRewardInstructionData
> {
  return mapSerializer<
    ClaimRewardInstructionDataArgs,
    any,
    ClaimRewardInstructionData
  >(
    struct<ClaimRewardInstructionData>(
      [['discriminator', array(u8(), { size: 8 })]],
      { description: 'ClaimRewardInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: [149, 95, 181, 242, 94, 90, 158, 162],
    })
  ) as Serializer<ClaimRewardInstructionDataArgs, ClaimRewardInstructionData>;
}

// Instruction.
export function claimReward(
  context: Pick<Context, 'programs'>,
  input: ClaimRewardInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'stakingProgram',
    '58LCGWxNcN1dsbDaWpR4YMNxVdSA8mx7pC8z59k6nfCA'
  );

  // Accounts.
  const resolvedAccounts = {
    user: { index: 0, isWritable: true as boolean, value: input.user ?? null },
    vaultAuthority: {
      index: 1,
      isWritable: false as boolean,
      value: input.vaultAuthority ?? null,
    },
    nftStake: {
      index: 2,
      isWritable: true as boolean,
      value: input.nftStake ?? null,
    },
    mint: { index: 3, isWritable: false as boolean, value: input.mint ?? null },
    userToken: {
      index: 4,
      isWritable: true as boolean,
      value: input.userToken ?? null,
    },
    vaultToken: {
      index: 5,
      isWritable: true as boolean,
      value: input.vaultToken ?? null,
    },
    tokenProgram: {
      index: 6,
      isWritable: false as boolean,
      value: input.tokenProgram ?? null,
    },
    systemProgram: {
      index: 7,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.tokenProgram.value) {
    resolvedAccounts.tokenProgram.value = context.programs.getPublicKey(
      'splToken',
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'
    );
    resolvedAccounts.tokenProgram.isWritable = false;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getClaimRewardInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
